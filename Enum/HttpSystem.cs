namespace Yove.Http
{
    /// <summary>
    /// Предоставляет OS для генерации UserAgent / Provides OS for generating UserAgent
    /// </summary>
    public enum HttpSystem
    {
        Linux,
        Mac,
        Windows,
        ChromeOS
    }
}